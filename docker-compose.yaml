
services:
  nginx:
    build: .
    container_name: my-local-nginx
    ports:
      - "80:80"      # HTTP
      - "443:443"    # HTTPS
      - "8084:8084"  # Второй HTTP сервер
      - "8081:8081"  # PHP сервер
    volumes:
      # Монтирование для разработки
      - ./site:/var/www/drug.ru
      - ./nginx-config/drug.ru.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php-fpm
    networks:
      - web-network

  php-fpm:
    image: php:8.2-fpm
    container_name: my-local-php
    volumes:
      - ./site:/var/www/drug.ru
    networks:
      - web-network

networks:
  web-network:
    driver: bridge